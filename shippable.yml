
jobs:
 - name: u14
   type: runSh
   steps:
     - TASK:
         runtime:
           options:
             imageName: "drydock/u14"
             imageTag: master
         script:
         - java -version
         - nodejs --version
         - source /usr/local/rvm/scripts/rvm
         - ruby -v
         - python -version
       
